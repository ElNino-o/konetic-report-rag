# ── Streamlit Cloud 배포용 (런타임 only) ──
# Streamlit Cloud 는 uv-네이티브 pyproject.toml 을 직접 읽지 못하므로(Poetry 형식으로 오인),
# 클라우드는 이 requirements.txt 를 사용한다. 내용은 pyproject [project.dependencies] 미러.
#
# 로컬 개발은 uv 사용:
#   uv sync --extra indexing      # 런타임 + 인덱싱(파싱/Chroma) 의존성
#   uv run streamlit run app.py

streamlit>=1.36
openai>=1.40
python-dotenv>=1.0
numpy>=1.26
rank-bm25>=0.2.2
