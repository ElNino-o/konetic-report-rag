# ── Streamlit Cloud 배포용 (런타임 only) · 자동 생성 파일, 직접 편집 금지 ──
# Streamlit Community Cloud 는 uv-native pyproject.toml / uv.lock 을 의존성 소스로
# 인식하지 못하므로(streamlit#9502) 이 requirements.txt 가 별도로 필요하다.
# pyproject [project.dependencies] 의 전이 의존성을 핀 고정한 산출물이다.
#
# 재생성(pyproject 의존성 변경 시):
#   uv pip compile --universal --no-annotate --no-header pyproject.toml -o requirements.txt
#
# FastAPI 백엔드(backend/main.py) 전용 의존성은 backend/requirements.txt 참고
# (Streamlit Cloud 는 app.py 만 실행하므로 fastapi 는 여기 포함하지 않는다).

altair==6.2.2
annotated-types==0.7.0
anyio==4.14.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.2
colorama==0.4.6 ; sys_platform == 'win32'
distro==1.9.0
gitdb==4.0.12
gitpython==3.1.50
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
itsdangerous==2.2.0
jinja2==3.1.6
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
markupsafe==3.0.3
narwhals==2.23.0
numpy==2.5.0
openai==2.44.0
packaging==26.2
pandas==3.0.3
pillow==12.3.0
protobuf==7.35.1
pyarrow==24.0.0
pydantic==2.13.4
pydantic-core==2.46.4
pydeck==0.9.2
pymupdf==1.28.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
rank-bm25==0.2.2
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
smmap==5.0.3
sniffio==1.3.1
starlette==1.3.1
streamlit==1.58.0
tenacity==9.1.4
toml==0.10.2
tqdm==4.68.3
typing-extensions==4.15.0
typing-inspection==0.4.2
tzdata==2026.2 ; sys_platform == 'emscripten' or sys_platform == 'win32'
urllib3==2.7.0
uvicorn==0.49.0
watchdog==6.0.0 ; sys_platform != 'darwin'
websockets==16.0
